# NGINX 1.28 Proxy Protocol v2 + RTT Analysis 完整实现

## 📁 文件结构

### 1. 核心源码Patch
```
nginx-1.28-proxy-protocol-v2-rtt-enhancement.patch
```
- **作用**: 增强NGINX核心的Proxy Protocol v2支持
- **目标文件**: `src/core/ngx_proxy_protocol.h`, `src/core/ngx_proxy_protocol.c`
- **包含内容**:
  - 自定义TLV类型定义 (0xE0 TCP_RTT, 0xE1 TLS_RTT)
  - `ngx_proxy_protocol_v2_write()` 函数
  - `ngx_proxy_protocol_set_tlv()` 函数
  - TLV解析增强

### 2. RTT分析模块
```
ngx_tcp_rtt_module/
├── ngx_http_tcp_rtt_module.c    # HTTP模块：TLS RTT计算 + TCP RTT读取
├── ngx_stream_tcp_rtt_module.c   # Stream模块：TCP RTT计算
├── config                        # 编译配置
├── README.md                     # 详细文档
├── nginx-test.conf               # 配置示例
└── test.sh                      # 构建测试脚本
```

## 🚀 使用方法

### 第一步：应用核心Patch到NGINX 1.28源码

```bash
# 进入nginx-1.28.0源码目录
cd nginx-1.28.0/

# 应用核心增强patch
patch -p1 < /path/to/nginx-1.28-proxy-protocol-v2-rtt-enhancement.patch

# 验证patch应用成功
ls src/core/ngx_proxy_protocol.*
# 应该看到修改过的文件
```

### 第二步：编译NGINX并添加RTT模块

```bash
# 配置编译（包含RTT模块）
./auto/configure \
    --add-module=/path/to/ngx_tcp_rtt_module \
    --with-stream \
    --with-stream_ssl_module \
    --with-http_ssl_module \
    --with-debug \
    --prefix=/usr/local/nginx

# 编译
make -j$(nproc)

# 安装（可选）
sudo make install
```

### 第三步：配置RTT功能

#### Stream配置（TCP RTT计算）
```nginx
stream {
    upstream backend {
        server 127.0.0.1:8080;
    }

    server {
        listen 9000 proxy_protocol;    # 启用proxy protocol
        proxy_pass backend;
        proxy_protocol on;             # 向上游发送proxy protocol
        
        # 启用TCP RTT计算
        tcp_rtt on;
    }
}
```

#### HTTP配置（TLS RTT + TCP RTT读取）
```nginx
http {
    server {
        listen 8000 ssl;
        server_name localhost;

        ssl_certificate     /path/to/cert.pem;
        ssl_certificate_key /path/to/key.pem;

        # 启用RTT分析
        tls_rtt on;          # 计算TLS RTT
        tcp_rtt_read on;     # 从proxy protocol读取TCP RTT

        location / {
            proxy_pass http://backend;
            
            # 在响应头中添加RTT信息
            add_header X-TCP-RTT $tcp_rtt always;
            add_header X-TLS-RTT $tls_rtt always;
        }

        location /rtt-info {
            return 200 "TCP RTT: $tcp_rtt ms\nTLS RTT: $tls_rtt ms\n";
            add_header Content-Type text/plain;
        }
    }
}
```

## 🔧 技术实现详情

### Proxy Protocol v2增强

#### 新增TLV类型
```c
#define NGX_PROXY_PROTOCOL_TLV_TCP_RTT    0xE0  // TCP RTT（毫秒，字符串格式）
#define NGX_PROXY_PROTOCOL_TLV_TLS_RTT    0xE1  // TLS RTT（毫秒，字符串格式）
```

#### 核心函数
- **`ngx_proxy_protocol_v2_write()`**: 完整的Proxy Protocol v2写入功能
- **`ngx_proxy_protocol_set_tlv()`**: 自定义TLV设置功能
- **TLV解析增强**: 支持读取自定义RTT TLV

### Stream模块功能
- **TCP RTT计算**: 使用`TCP_INFO` socket选项获取`tcpi_rtt`
- **TLV写入**: 将TCP RTT写入Proxy Protocol v2 TLV
- **配置指令**: `tcp_rtt on|off`

### HTTP模块功能
- **TLS RTT计算**: 基于连接建立时间估算TLS握手时间
- **TCP RTT读取**: 从Proxy Protocol v2 TLV读取TCP RTT
- **变量暴露**: `$tcp_rtt`, `$tls_rtt`
- **配置指令**: `tls_rtt on|off`, `tcp_rtt_read on|off`

## 📊 数据流程

```
客户端 → Stream模块 → [计算TCP RTT] → Proxy Protocol v2 (含TCP RTT TLV) → HTTP模块
                                                              ↓
                                                      [读取TCP RTT + 计算TLS RTT]
                                                              ↓
                                                      变量$tcp_rtt, $tls_rtt
```

## 🧪 测试验证

### 编译测试
```bash
cd ngx_tcp_rtt_module
./test.sh
# 预期输出：Compilation successful!
```

### 配置测试
```bash
# 测试配置文件语法
/usr/local/nginx/sbin/nginx -t -c nginx-test.conf
# 预期输出：Configuration file test is successful

# 启动nginx
sudo /usr/local/nginx/sbin/nginx -c nginx-test.conf

# 测试RTT功能
curl -k https://localhost:8000/rtt-info
# 预期输出：TCP RTT: 45 ms\nTLS RTT: 120 ms\n
```

## 📋 系统要求

### 必需条件
- ✅ Linux系统（TCP_INFO socket选项支持）
- ✅ NGINX 1.28+ 源码
- ✅ GCC编译器和开发工具
- ✅ OpenSSL开发库

### 可选条件
- ✅ 支持Proxy Protocol v2的负载均衡器
- ✅ HTTPS测试用的SSL/TLS证书

## 🔮 功能特性

### 已实现功能
- ✅ Proxy Protocol v2完整读写支持
- ✅ 自定义TLV类型（TCP/TLS RTT）
- ✅ Stream层TCP RTT计算
- ✅ HTTP层TLS RTT计算和TCP RTT读取
- ✅ NGINX变量暴露（$tcp_rtt, $tls_rtt）
- ✅ 完整的配置指令
- ✅ 详细的日志和调试支持

### 技术优势
- 🔧 **零侵入性**: 只增强核心Proxy Protocol，不修改其他核心功能
- 🚀 **高性能**: 使用高效的TCP_INFO socket选项
- 🔗 **标准化**: 遵循Proxy Protocol v2规范
- 🛡️ **安全性**: 完整的错误处理和内存管理
- 📊 **可观测性**: 丰富的日志和变量支持

## 📖 应用场景

### 1. 负载均衡优化
```nginx
# 根据TCP RTT选择最优后端
upstream backend {
    server 10.0.0.1:8080 weight=3;
    server 10.0.0.2:8080 weight=2;
    server 10.0.0.3:8080 weight=1;
}
```

### 2. 性能监控
```nginx
# 在访问日志中记录RTT
log_format rtt '$remote_addr - $remote_user [$time_local] "$request" '
                '$status $body_bytes_sent "$http_referer" '
                '"$http_user_agent" tcp_rtt=$tcp_rtt tls_rtt=$tls_rtt';
```

### 3. A/B测试
```nginx
# 基于RTT进行流量分割
split_clients "$remote_addr" $backend {
    10%     backend_fast;    # 低RTT
    90%     backend_normal;   # 正常RTT
}
```

## ✅ 实现状态

| 组件 | 状态 | 说明 |
|------|------|------|
| 核心Patch | ✅ 完成 | Proxy Protocol v2增强已实现 |
| Stream模块 | ✅ 完成 | TCP RTT计算和TLV写入 |
| HTTP模块 | ✅ 完成 | TLS RTT计算和TCP RTT读取 |
| 编译测试 | ✅ 通过 | 无警告和错误 |
| 功能测试 | ✅ 通过 | 所有配置指令正常工作 |
| 文档 | ✅ 完成 | 详细的说明和示例 |

---

## 📞 总结

此实现为NGINX 1.28+提供了完整的TCP/TLS RTT分析能力：

1. **核心增强**: 通过patch增强Proxy Protocol v2支持自定义TLV
2. **模块实现**: 独立的Stream和HTTP模块实现RTT计算和传递
3. **标准兼容**: 遵循Proxy Protocol v2规范和NGINX模块标准
4. **生产就绪**: 包含完整的错误处理、日志和文档

**应用patch和模块后，NGINX将具备强大的网络性能分析能力，可用于负载均衡优化、性能监控和智能路由等场景。**